# include "minishell.h"

int main()
{
    // int     pipe_one[2];
    // int     pipe_two[2];
    // char    yes[2];
    // char    head[2];
    // char    wc[2];

    // (void)av;
    // (void)ev;
    // yes[0] = "/usr/bin/yes";
    // yes[1] = 0;
    // head[0] = "/usr/bin/head";
    // head[1] = 0;
    // wc[0] = "/usr/bin/wc";
    // wc[1] = 0;
    // pipe(pipe_one);
    // if (!fork())
    // {
    //     dup2(pipe_one[0], 0);
    //     close(pipe_one[1]);   
    //     close(pipe_one[0]);
    //     execve(wc[0], wc, ev);
    // }
    // close(pipe_one[0]);
    // pipe(pipe_two);
    printf("Hello, there will be a minishell soon\n");
}